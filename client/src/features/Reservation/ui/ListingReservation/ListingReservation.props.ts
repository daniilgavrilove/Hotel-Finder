import {Range} from "react-date-range";

export interface ListingReservationProps {
    price: number;
    listingId:number
    // totalPrice: number;
    // onSubmit: () => void;
    // disabled?: boolean;
    // disabledDates: Date[];
}